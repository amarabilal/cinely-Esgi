<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEBUG BUILDS ONLY (src/debug). Release builds use the platform default (HTTPS-only). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
